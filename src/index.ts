/**
 * Omni Worker v2.0 — Main entry point
 *
 * Exports the public API for creating and managing workers
 * across Node.js and web environments.
 *
 * @packageDocumentation
 */
export * from './types';

/**
 * Create an Omni Worker instance.
 *
 * @throws Error - Not yet implemented in v2.0 scaffolding.
 * @remarks Full implementation will be provided in a subsequent task.
 */
export function omniWorker(): never {
  throw new Error('omniWorker: Not implemented yet');
}

/**
 * Create an Omni Worker Pool instance.
 *
 * @throws Error - Not yet implemented in v2.0 scaffolding.
 * @remarks Full implementation will be provided in a subsequent task.
 */
export function omniWorkerPool(): never {
  throw new Error('omniWorkerPool: Not implemented yet');
}
